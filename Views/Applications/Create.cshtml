@model JobTrackr.Models.JobApplication
@{ ViewData["Title"] = "Add application"; }
<div class="page-heading"><a asp-action="Index" class="text-decoration-none small">← Back to applications</a><h1 class="fw-bold mt-2">Add an opportunity</h1><p class="text-muted">Capture the details while they are fresh.</p></div><form asp-action="Create" class="form-card"><partial name="_ApplicationForm" model="Model" /><div class="mt-4"><button class="btn btn-primary px-4">Save application</button><a asp-action="Index" class="btn btn-link text-muted">Cancel</a></div></form>
